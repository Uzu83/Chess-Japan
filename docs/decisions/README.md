# docs/decisions — 不可逆な設計判断の記録先

- ここには**不可逆な設計判断**（アーキ変更・データ削除を伴う migration・課金導入 等）を記録する。
- 1判断 = 1ファイル。ファイル名は `YYYY-MM-DD-<slug>.md`。
- 過去の判断（Next.js 不採用・コスト防衛設計 等）は `docs/PLAN.md` に記録済みで、そのまま（移植しない）。
